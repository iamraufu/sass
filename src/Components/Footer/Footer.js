import React from 'react';
import styles from './Footer.module.css';

const Footer = () => {
      return (
            <div>
                  <p className={styles.title}><small>This is Footer</small></p>      
            </div>
      );
};

export default Footer;  